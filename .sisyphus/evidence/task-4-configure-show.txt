Task 4 — configure --show (readCurrentConfig) evidence
========================================================

Command run:
  dart test test/cli/commands/configure_command_test.dart --name "show|--show|readCurrentConfig"

Output:
  Running build hooks...
  00:00 +0: loading test/cli/commands/configure_command_test.dart
  00:00 +0: readCurrentConfig parses app_id from push section
  00:00 +1: readCurrentConfig parses polling_interval from database section
  00:00 +2: readCurrentConfig parses database enabled flag
  00:00 +3: readCurrentConfig parses soft_prompt enabled flag
  00:00 +4: readCurrentConfig throws when config file does not exist
  00:00 +5: updateConfig app_id updated app_id is readable by readCurrentConfig
  00:00 +6: updateConfig polling_interval updated polling_interval is readable by readCurrentConfig
  00:00 +7: All tests passed!

Verified:
  - readCurrentConfig correctly parses app_id, polling_interval, enabled flags
  - Throws FileSystemException when config is missing
  - Uses FileHelper.readFile() internally — no direct File() calls
